# Copy this to .env.local for local development
# Leave VITE_API_URL unset locally — Vite dev server proxies /api to localhost:5000

# Required for production (set this in Vercel dashboard, NOT in .env.local)
VITE_API_URL=https://study-mate-ai-7cpy.onrender.com/api
